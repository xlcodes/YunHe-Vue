---
alwaysApply: true
scene: git_message
---

AI 生成的 Git 提交信息必须遵循以下规则：

- 生成的提交信息必须是中文，不能是纯英文
- 严格遵循 Angular 规范，仅输出单行提交信息：type: 简短描述
- 只输出一行，不换行、不加详情、不列改动、无任何多余文字
- 中文与英文、中文与数字相邻两侧，必须保留 1 个半角空格
- 描述文字严格控制总长度，整行 commit 标题不超过 20 字符，精简措辞绝不超限
